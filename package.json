{
  "name": "@policies2/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for executing policies and flows over REST.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "build:examples": "tsc --module NodeNext --moduleResolution NodeNext --target ES2022 --outDir dist/examples examples/*.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "prepublishOnly": "tsc"
  },
  "keywords": [
    "policy",
    "sdk",
    "typescript",
    "rest"
  ],
  "license": "MIT",
  "dependencies": {
    
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.9.3"
  }
}
